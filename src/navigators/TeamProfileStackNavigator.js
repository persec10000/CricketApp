import {createStackNavigator} from 'react-navigation-stack';

import TeamProfileStackRoutes from './routes/TeamProfileStackRoutes';

const TeamProfileStackNavigator = createStackNavigator(TeamProfileStackRoutes);

export default TeamProfileStackNavigator;
