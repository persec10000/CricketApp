import {createStackNavigator} from 'react-navigation-stack';

import SetupStackRoutes from './routes/SetupStackRoutes';

const SetupStackNavigator = createStackNavigator(SetupStackRoutes);

export default SetupStackNavigator;
